[project]
name = "democraz"
version = "0.2.0"
description = "Uma alternativa para o site do governo brasileiro da Câmara dos Deputados"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.2",
]
